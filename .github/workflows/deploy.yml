name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build and test with coverage check
        run: ./gradlew check

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: PHYSICAL_SERVER_PASSWORD,JWT_SECRET,DB_PASSWORD
          script: |
            cd ~/duoinfra-backend
            git pull origin main
            chmod +x gradlew
            ./gradlew bootJar
            docker compose up -d --build
        env:
          PHYSICAL_SERVER_PASSWORD: ${{ secrets.PHYSICAL_SERVER_PASSWORD }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      - name: Notify Discord on success
        if: success()
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content": "✅ 배포 성공 - ${{ github.repository }} (${{ github.sha }})"}' \
          ${{ secrets.DISCORD_WEBHOOK }}

      - name: Notify Discord on failure
        if: failure()
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content": "❌ 배포 실패 - ${{ github.repository }} (${{ github.sha }})"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
